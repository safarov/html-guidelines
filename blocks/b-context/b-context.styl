@import '../config';

.b-context {
    margin-top: (baseLineHeight*1.5);
    margin-bottom: baseLineHeight;
    border-left: 2px solid #c00;
    padding: (baseLineHeight / 2) (baseLineHeight - 2px);
}